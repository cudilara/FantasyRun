<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorTop</item>
        <item name="colorPrimaryDark">@color/colorBottomMenu</item>
        <item name="colorAccent">@color/colorScreenStart</item>
    </style>

    <style name="BlueTheme" parent="Theme.Design.NoActionBar">
        <item name="colorPrimary">@color/colorTop</item>
        <item name="android:windowBackground">@color/colorScreenStart</item>
    </style>
</resources>
